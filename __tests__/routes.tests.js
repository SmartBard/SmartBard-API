describe('TESTS: Announcement Route', () => {
    beforeEach(() => {
        
    })

    afterEach(() => {
        jest.clearAllMocks();
    })

})

describe('TESTS: Assets Route', () => {
    beforeEach(() => {
        
    })

    afterEach(() => {
        jest.clearAllMocks();
    })

})

describe('TESTS: Auditlog Route', () => {
    beforeEach(() => {
        
    })

    afterEach(() => {
        jest.clearAllMocks();
    })

})

describe('TESTS: Index Route', () => {
    beforeEach(() => {
        
    })

    afterEach(() => {
        jest.clearAllMocks();
    })

})

describe('TESTS: User Settings Route', () => {
    beforeEach(() => {
        
    })

    afterEach(() => {
        jest.clearAllMocks();
    })

})

describe('TESTS: Users Route', () => {
    beforeEach(() => {
        
    })

    afterEach(() => {
        jest.clearAllMocks();
    })

})